rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /recipes/{userId}/{fileName} {
      // Allow reads for everyone
      allow read: if true;
      // Allow writes only for the authenticated user who owns the path
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
